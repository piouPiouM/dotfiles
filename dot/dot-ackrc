# Always sort the files
--sort-files

# Always groups matches by file name with
--group

# Ignore Sass cache directory
--ignore-dir=.sass-cache,.vendor,.vendors,.bundle

--ignore-dir=is:node_modules
--ignore-dir=is:bower_components

# Define Sass and SCSS types
--type-set=sass=ext:sass,scss

# Define Markdown
--type-set=markdown=ext:md,markdown

# Define Drupal 7
--type-set=d7=ext:php,tpl.php,module,inc,install,view,engine,info
